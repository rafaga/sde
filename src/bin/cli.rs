//! Utilería de línea de comandos para (re)generar `sde.db`.
//!
//! Por ahora es un esqueleto: el parseo de argumentos y la orquestación real
//! se agregan junto con `sde::builder`. Requiere la feature `builder`.

fn main() -> anyhow::Result<()> {
    println!("sde-cli: aún no implementado. La lógica de build vive en sde::builder.");
    Ok(())
}
